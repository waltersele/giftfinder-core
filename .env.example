# ============================================================================
# GIFTIA ENVIRONMENT CONFIGURATION
# ============================================================================
# 
# IMPORTANTE: Copia este archivo a .env y completa con tus valores reales.
# 
# UBICACIÓN RECOMENDADA DEL ARCHIVO .env:
# - WordPress Root: /path/to/wordpress/.env
# - O en: /wp-content/.env
# - O en: /var/www/html/.env
#
# La aplicación buscará en este orden:
# 1. WordPress Root (.env)
# 2. wp-content/ (.env)
# 3. Plugin directory (giftfinder-core/.env)
# 4. DOCUMENT_ROOT (.env)
# 5. /var/www/html/ (.env)
#
# ============================================================================

# TOKEN DE AUTENTICACIÓN DE API
# Genera un valor seguro: https://www.uuidgenerator.net/ o usa: 
# python3 -c "import secrets; print(secrets.token_hex(32))"
WP_API_TOKEN=tu_token_secreto_super_seguro_aqui_32_caracteres

# CLAVE API DE GOOGLE GEMINI
# Obtén tu clave en: https://aistudio.google.com/app/apikeys
# Permite generar descripciones y títulos automáticos vía IA
GEMINI_API_KEY=AIzaSyD_tu_clave_api_gemini_aqui_muy_larga

# ID DE AFILIADO DE AMAZON
# Tu código de etiqueta de asociado (ej: tusitio-21, tudominio-21)
# Se añade automáticamente a todos los enlaces de productos
AMAZON_TAG=GIFTIA-21

# ORÍGENES CORS PERMITIDOS (JSON Array)
# Dominios que pueden hacer solicitudes a la API desde el navegador
# Separados por coma, sin espacios
ALLOWED_ORIGINS=["https://giftia.es","https://www.giftia.es","https://app.giftia.es"]

# URL DE LA API DE INGESTA
# Por defecto apunta al plugin instalado, cambiar si está en otra ubicación
WP_API_URL=https://giftia.es/wp-content/plugins/giftfinder-core/api-ingest.php

# MODO DEBUG
# 1 = Habilita logs detallados en wp-content/giftia-debug.log
# 0 = Solo errores críticos
DEBUG=0

# ============================================================================
# CONFIGURACIÓN AVANZADA (Opcional)
# ============================================================================

# TIMEOUT DE SOLICITUDES (segundos)
REQUEST_TIMEOUT=30

# LÍMITE DE RATE LIMITING (solicitudes por hora por IP)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600

# VALIDACIÓN ESTRICTA DE HTTPS
# Rechaza imágenes y URLs en HTTP (recomendado en producción)
ENFORCE_HTTPS=1

# NIVEL DE LOG
# debug | info | warning | error | critical
LOG_LEVEL=info

# ============================================================================
# INSTRUCCIONES DE CONFIGURACIÓN
# ============================================================================
#
# 1. OBTENER GIFTIA_API_TOKEN:
#    - Genera en WordPress: Panel Admin → Giftia Settings
#    - O genera manualmente: python -c "import secrets; print(secrets.token_hex(32))"
#
# 2. OBTENER GEMINI_API_KEY:
#    - Ve a: https://aistudio.google.com/app/apikeys
#    - Crea una nueva clave de API
#    - Copia aquí
#
# 3. AMAZON_TAG:
#    - Obtén en: https://associates.amazon.es
#    - Formato típico: tudominio-21 (donde 21 es código de región ES)
#
# 4. ALLOWED_ORIGINS:
#    - Añade todos los dominios que usarán la API
#    - Formato JSON: ["https://domain1.com","https://domain2.com"]
#    - Tu dominio WordPress se incluye automáticamente
#
# 5. PERMISOS DEL ARCHIVO:
#    chmod 644 .env    # Legible pero no escribible por scripts externos
#    
#    En algunos hostings compartidos, puede ser necesario:
#    chmod 666 .env    # Solo si el hoster lo requiere
#
# ============================================================================
